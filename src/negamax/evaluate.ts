import { Node, Context, FeatureDelta, MoveDelta } from './types'

function run_context(ctx: Context) {

}

export function evaluate(node: Node) {
    return 0

}

export function generate_moves(ctx: Context, side: boolean): MoveDelta[] {
    run_context(ctx)

    return []
}