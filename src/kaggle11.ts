import { PositionGroup } from "./kaggle10";
import { Color } from "./types";

export function match_group(g: PositionGroup, lowers_turn: Color): [PositionGroup, PositionGroup] {
    let aa: PositionGroup = [],
    bb: PositionGroup = []
    


    return [aa, bb]
}