if has_fork_with_checks_for_only_king_evades_and_discovered_attack(bishop, bishop2, king, rook, rook2, rook3) then
  forall king_evades(king, king2) then
    if captures(rook2, rook3_rook4) then



if has_fork_with_checks_for_only_king_evades_and_discovered_attack(bishop, bishop2, king, rook, queen, queen2) then
  forall king_evades(king, king2) then
    if captures(queen, queen2_queen3) then

if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then
      if captures(knight, bishop4_knight2) then
        if captures(rook2, knight2_rook3) then


if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then
      forall has_no_captures(rook2) then

if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if checkmate(rook2, rook3, king2) then



if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then
      if checks(rook2, rook3, king3) then
        if captures(rook4, rook3_rook5) then
        if king_evades(king3, king4) then


if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if has_check_with_discovered_attack_on_hanging(bishop3, bishop4, king2, rook2, queen) then
      forall king_evades(king2, king3) then
        if captures(rook2, queen_rook3) then

if has_fork_with_checks_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then
      if captures(knight, bishop4_knight2) then
        if captures(pawn, knight2_pawn2) then
      if captures(rook2, bishop3_rook3) then
        if captures(rook4, pawn_rook5) then
        
if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then




if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades_and_attacks_hanging(king, king2, rook2) then
    if checks_defended_by(rook2, rook3, king2, bishop2) then
      forall king_evades(king2, king3) then
        if captures(bishop2, rook_bishop3) then

if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades_and_attacks_hanging(king, king2, rook2) then
    if checks_defended_by(rook2, rook3, king2, bishop2) then

if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if captures(bishop2, rook_bishop3) then

if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then

###


if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then

if has_fork_with_checks_and_hanging_for_only_king_evades(bishop, bishop2, king, rook) then
  forall king_evades(king, king2) then
    if checks_defended_by_also_evades_attack(rook2, rook3, king2, bishop2, knight) then
    if checks_defended_by_also_evades_attack(rook2, rook3, king2, bishop2, bishop3) then
      forall king_evades(king2, king3) then
        if captures(bishop2, rook_bishop4) then
    if checks_defended_by_also_evades_attack(rook2, rook3, king2, bishop2, king2) then
      forall king_evades(king2, king3) then
        if captures(bishop2, rook_bishop4) then
    if checks_defended_by_also_evades_attack(rook2, rook3, king2, bishop2, bishop3) then
    if checks_defended_by_also_evades_attack(rook2, rook3, king2, bishop2, king2) then
    if captures(bishop2, rook_bishop3) then


###

def checks_defended_by_also_evades_attack(From, To, King, Defender, Attacker) then
  attack(Attacker, From)
  move(From, To)
  attack(To, King)


def checks_defended_by(From, To, King, Defender)
  move(From, To)
  attack(To, King)
  defend(Defender, To)

def has_no_captures(To)
  no_captures(To)

def has_check_with_discovered_attack_on_hanging(From, To, King, DiscoverFrom, DiscoverTo)
  attack_through(DiscoverFrom, DiscoverTo, To)
  move(From, To)
  attack(To, King)
  attack(DiscoverFrom, DiscoverTo)

def checks(From, To, King) then
  move(From, To)
  attack(To, King)

def captures(From, Captured_To)
  capture(From, To, Captured)


def has_fork(From, To, ForkA, ForkB)
  move(From, To)
  attack(To, ForkA)
  attack(To, ForkB)

def checkmate(From, To, King)
  move(From, To)
  attack(To, King)
  no_king_evades(King)
  no_captures(To)
  no_blocks_check(To, King)
  no_push_blocks_check(To, King)

def checks_for_only_king_evades(From, To, King)
  move(From, To)
  attack(To, King)
  no_captures(To)
  no_blocks_check(To, King)
  no_push_blocks_check(To, King)

def king_evades(From, To)
  safe_move(From, To)


def king_evades_and_attacks_hanging(From, To, Hanging)
  safe_move(From, To)
  attack(To, Hanging)
  no_defense(Hanging)


def has_fork_with_checks_for_only_king_evades(From, To, King, ForkB)
  move(From, To)
  attack(To, King)
  attack(To, ForkB)
  no_captures(To)
  no_blocks_check(To, King)
  no_push_blocks_check(To, King)


def has_fork_with_checks_and_hanging_for_only_king_evades(From, To, King, ForkB)
  move(From, To)
  attack(To, King)
  attack(To, ForkB)
  no_captures(To)
  no_blocks_check(To, King)
  no_push_blocks_check(To, King)
  no_defense(ForkB)




  
def has_fork_with_checks_for_only_king_evades_and_discovered_attack(From, To, King, ForkB, DiscoverFrom, DiscoverTo)
  attack_through(DiscoverFrom, DiscoverTo, To)
  move(From, To)
  attack(To, King)
  attack(To, ForkB)
  no_captures(To)
  no_blocks_check(To, King)
  no_push_blocks_check(To, King)
  attack(DiscoverFrom, DiscoverTo)