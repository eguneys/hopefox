if has_fork(bishop, bishop2, rook, king) then
 if king_evades(king, king2) then
   if captures(bishop2, rook_bishop3) then

if has_fork(bishop, bishop2, rook, king) then

if has_fork(queen, queen2, rook, king) then

if blocks_check(knight, knight2, rook, king) then
 if captures(rook, knight2_rook2) then
   if captures(pawn, rook2_pawn2) then
     if checks(queen, queen2, king2) then
       if captures(queen3, queen2_queen4) then
         if has_fork(bishop, bishop2, king2, queen4) then
           if captures(queen4, bishop2_queen5) then

if has_fork(bishop, bishop2, rook, king) then
   if captures(king, bishop2_king2) then
     if discovered_check(knight, knight2, rook2, king2) then
       if king_evades(king2, king3) then
         if captures(knight2, queen_knight3) then

if attacks_hanging(queen, queen2, knight) then
  if evades(knight, knight2) then
    if captures(knight3, knight2_knight4) then
      if captures(rook, knight4_rook2) then
        if captures(queen2, rook2_queen3) then
##

def evades(From, To)
  move(From, To)

def attacks_hanging(From, To, Hanging)
  attack(null, Hanging)
  move(From, To)
  attack(To, Hanging)

def discovered_check(From, To, AFrom, ATo)
  attack_through(AFrom, ATo, From) and
  move(From, To) then
  attack(AFrom, ATo)

def checks(From, To, King) then
  move(From, To) then
  attack(To, King)

def blocks_check(From, To, AFrom, ATo)
  attack(AFrom, ATo) and
  move(From, To) then
  attack_through(AFrom, ATo, To)

def captures(From, Captured_To)
  capture(From, To, Captured) then

def king_evades(From, To) then
  move(From, To) then
  

def has_fork(From, To, ForkA, ForkB)
  move(From, To) then
  attack(To, ForkA) and
  attack(To, ForkB)